[project]
name = "grayscale-openevolve"
version = "0.1.0"
description = "OpenEvolve baseline for grayscale CUDA kernel optimization on H100"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.0.0",
    "modal>=0.73.0",
    "openevolve>=0.2.0",
]
